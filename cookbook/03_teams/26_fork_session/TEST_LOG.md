# Team Fork Session — Test Log

### 01_fork_session.py

**Status:** NOT RUN (requires OPENAI_API_KEY)

**Description:** `team.fork_session()` / `afork_session()` and `forked_from_session_id` lineage across nested forks.

**Result:** Syntax/compile verified. Session-fork behavior covered by `test_team_checkpointing.py` (`TestTeamRunOutputLineage`, fork tests).

---
